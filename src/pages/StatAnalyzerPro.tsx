import React from 'react';
import { StatAnalyzerLayout } from '../components/stat-analyzer/StatAnalyzerLayout';

export function StatAnalyzerPro() {
  return (
    <StatAnalyzerLayout>
      {/* Content will be added in subsequent steps */}
    </StatAnalyzerLayout>
  );
}
